# Parte 1 


> [!tldr]- Parte 1
> ![[Q1.pdf]]

## Exercício 1 
Defina um programa que lê (usando a função scanf uma sequência de números inteiros terminada com o número 0 e imprime no ecran o maior elemento da sequência.

```c
int maior(int a[], int size){
    int max = a[0];
    for (int i = 1; i < size; i++){
        if (a[i] > max){
            max = a[i];
        } 
    }
    return max;
} 
```

```c title:'main para teste'
int main(){
    //Recolhe número de elementos do array
    int num;
    printf("Quantos números terá sua sequência?");
    scanf("%d", &num);

    // cria o array e coleta os valores 
    int arr[num];
    readInArray(arr, num);

    // reetira o maior elemnto do array
    int m = maior(arr, num);
    printf("%d", m);
    
    return 0;
}
```

## Exercício 2 
Defina um programa que lê (usando a função scanf uma sequência de números inteiros terminada com o número 0 e imprime no ecran a média da sequência.

```c 
int soma (int v[], int N){
    int i; 
    int r = 0;
    for (i = 0; i < N; i++){
        r += v[i];
    }
    return r;
}

int media(int v[], int N){
    int sum = soma(v, N);
    int r = sum/2;
    return r;
}
```


## Exercício 3 
Defina um programa que lê (usando a função scanf uma sequência de números inteiros terminada com o número 0 e imprime no ecran o segundo maior elemento.

```c 
int secound_max(int a[], int N){
    int maximun = maior(a, N);
    int segMax = a[0];
    for (int i= 0; i < N; i++){
        if ((a[i] > segMax) && (a[i]< maximun)){
            segMax = a[i];
        }
    }
    return segMax; 
}
```

```c title:'main + auxiliar'
int main(){ 
    //Recolhe número de elementos do array
    int num;
    printf("Quantos números terá sua sequência?");
    scanf("%d", &num);

    // cria o array e coleta os valores 
    int arr[num];
    readInArray(arr, num);

    // reetira o maior elemnto do array
    int m = secound_max(arr, num);
    printf("O segundo maior número é : %d", m);

    putchar('\n'); 
    return 0;
}


void readInArray(int *arr, int size) {
    int i;
    printf("Enter your list of numbers: ");
    for (i = 0; i < size; i++) {
        scanf("%d", &arr[i]);
    }
}
```

## Exercício 4
Defina uma função int bitsUm (unsigned int n) que calcula o número de bits iguais a 1 usados na representação binária de um dado número n. (https://codeboard.io/projects/13548). 

```c
int bitsUm (unsigned int n){
    int unos = 0;
    for (int i = 1; i <= 32; i++){ // máximo de 32 bits
        if (n & 1) unos += 1;
        n = (n >> 1);
    }
    return unos; 
} 
```

```c title:'main para teste'
int main(){ 
    //Recolhe número 
    int num;
    printf("Digite o número?\n");
    scanf("%d", &num);

    // contagem dos 1s
    int r = bitsUm(num);
    printf("Em binário %d tem %d zeros", num, r);

    putchar('\n'); 
    return 0;
}
```

## Exercício 5 
Defina uma função int trailingZ (unsigned int n) que calcula o número de bits a 0 no final da representação binária de um número (i.e., o expoente da maior potência de 2 que é divisor desse número). (https://codeboard.io/projects/13549)

```c 
int trailingZ (unsigned int n){
    int zeros = 0;
    for (int i = 1; i <= 32; i++){ // máximo de 32 bits
        if (n & 1) break;
        else {
        zeros += 1;
        n = n >> 1;
        }
         
    }
    return zeros;
}
```

```c title:'main para testes'
int main(){ 
    //Recolhe número 
    int num;
    printf("Digite o número?\n");
    scanf("%d", &num);

    // contagem dos 0s
    int r = trailingZ(num);
    printf("Em binário %d tem %d zeros", num, r);

    putchar('\n'); 
    return 0;
}
```

## Exercício 6
 Defina uma função int qDig (unsigned int n) que calcula o número de dı́gitos necessários para escrever o inteiro n em base decimal. Por exemplo, qDig (440) deve retornar 3. 
 (https://codeboard.io/projects/13583)

```c 
int qDig (unsigned int n){
    int i;
    for (i = 1; i > 0; i++){
        if (n/10 == 0) break;
        n = n/10;
    }
    return i; 
}
```

```c title:main
int main(){ 
    //Recolhe número 
    int num;
    printf("Digite o número?\n");
    scanf("%d", &num);

    // contagem dos 0s
    int r = qDig(num);
    printf("É preciso %d bits para representar %d em base 10", r, num);

    putchar('\n'); 
    return 0;
}
```

## Exercício 7
Apresente uma definição da função pré-definida em C char *strcat (char s1[], char s2[]) que concatena a string s2 a s1 (retornando o endereço da primeira). (https://codeboard.io/projects/14490)

```c 
char *strcaT (char s1[], char s2[]){
    int i = 0, j = 0; 
    while (s1[i] != '\0') i++;
    while (s2[j] != '\0'){
        s1[i] = s2[j];
        j++;
        i++;
    }
    s1[i] = '\0';
    return s1;
}
```



